[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "16"
  NPM_VERSION = "8"

# Redirect all routes back to index.html for SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200 